using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Profiling;

public class Repo : MonoBehaviour {

	// Use this for initialization
	// void Start () {
	// 	// 複数のフラグを同時に上げ下げしないと正しく動作しない
	// 	Profiler.logFile = "profiler.log";
    //     Profiler.enableBinaryLog = true;
    //     Profiler.enabled = true;

    //     Debug.LogError("here! Profiler.enabled:" + Profiler.enabled);
	// }
	
	// // Update is called once per frame
	// void Update () {
		
	// }

	// void OnApplicationQuit () {
	// 	// 複数のフラグを同時に上げ下げしないと正しく動作しない
	// 	Profiler.logFile = string.Empty;
	// 	Profiler.enableBinaryLog = false;
	// 	Profiler.enabled = false;
	// 	Debug.LogError("exit! Profiler.enabled:" + Profiler.enabled);
	// }
}
